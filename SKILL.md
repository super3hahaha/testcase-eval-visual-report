---
name: testcase-eval-visual-report
version: 1.0.0
description: |
  评估 AI 生成测试用例的质量。当用户提供两个 HTML 表格文件（一个是 AI 原始生成的测试用例，另一个是人工修改后最终采纳的版本），通过 diff 对比提取修改内容，计算人工修改率，并生成可视化 HTML 差异报告。

  触发场景：
  - 用户提到"评估 AI 测试用例质量"、"测试用例修改率"、"对比 AI 生成和人工修改的用例"
  - 用户上传了两个 HTML 文件，且上下文与测试用例 / 用例评估相关
  - 用户说"帮我跑 diff"、"生成用例评估报告"、"看看 AI 生成的准确率"
  即使用户只说"帮我对比这两个文件"，只要上下文涉及测试用例，也应触发此 skill。
---

# AI 测试用例质量评估 Skill

通过对比【AI 原始生成的 HTML 测试用例表格】与【人工最终修改并采纳的 HTML 表格】，自动计算人工修改率并生成可视化 Diff 报告。

---

## 依赖

- Python 3
- `beautifulsoup4`（`pip install beautifulsoup4`）
- 脚本：`diff_testcases.py`（位于 `/mnt/user-data/uploads/` 或工作目录）

---

## 执行流程

### Step 1：确认文件身份

用户需提供**两个 HTML 文件**。在执行前，必须明确：

- 哪个文件是 **AI 生成的原始版本**
- 哪个文件是 **人工修改后的最终版本**

**如果用户没有说明两个文件分别是哪个角色**（例如只说"这两个文件帮我对比一下"），**必须先询问用户，再执行**，不得自行猜测：

> "请问哪个文件是 AI 生成的原始版本，哪个是人工修改后的最终版本？"

如果用户已在上传说明中注明（如文件名包含"AI"/"人工"/"original"/"modified"，或消息中有明确指定），则可直接执行，无需再问。

---

### Step 2：安装依赖

```bash
pip install beautifulsoup4 --break-system-packages -q
```

---

### Step 3：定位脚本

脚本位于 skill 的 `scripts/diff_testcases.py`，安装后可在 skill 目录下找到。

若不存在，告知用户上传 `diff_testcases.py`。

---

### Step 4：执行脚本

```bash
python <skill目录>/scripts/diff_testcases.py \
  "<AI生成文件路径>" \
  "<人工修改文件路径>" \
  -o /mnt/user-data/outputs/diff_report.html
```

- 输出报告默认为 `diff_report.html`
- 用户可通过 `-o` 指定自定义输出名
- 可选参数 `--ai-color #xxxxxx` / `--human-color #xxxxxx`：极端场景下手动覆盖颜色识别（一般无需使用）

---

### Step 5：呈现结果

脚本执行完成后，调用 `present_files` 工具展示 `/mnt/user-data/outputs/diff_report.html`。

---

## 报告说明

报告以 **AI 原始表格为基础**渲染，仅对红色文本部分做差异标注，黑色文本保持不变：

| 场景 | 标注样式 |
|---|---|
| 红色文本被人工删除 | 保留原文 + ~~删除线~~ + 红色淡色背景底 |
| 人工新增的文本 | 【新增文本】绿色字体 |
| 红色文本被人工修改 | 旧文本~~删除线~~紧跟新文本蓝色字体 |
| 整行被人工删除 | 所有单元格加删除线 + 整行灰色背景底 |
| 整行被人工新增 | 所有文本【】包裹 + 整行绿色背景底 |
| 红色文本未被修改 | 保持原红色显示，无额外标注 |
| 原黑色文本 | 保持不变 |

---

## 错误处理

| 错误情况 | 处理方式 |
|---|---|
| 未找到 `<table class='waffle'>` | 告知用户该 HTML 文件格式不符合要求（需为 Google Sheets 导出的 waffle 表格） |
| 文件路径不存在 | 提示用户确认上传路径 |
| beautifulsoup4 未安装 | 自动执行 pip install 后重试 |
| 两个文件身份不明 | 询问用户后再执行（见 Step 1） |

---

## 注意事项

- 脚本仅处理带有 `class="waffle"` 的 HTML 表格（Google Sheets 导出格式）
- **颜色识别**：脚本自动探测每个 HTML 文件里最常见的"非黑色文本色"作为"AI 生成内容标记"。AI 文件和人工文件**无需使用同一种红色**（`#ea4335` / `#ff0000` 等都可），脚本通过"主导 RGB 通道相同"判定同色系。如果人工文件中除红色外还有蓝色、绿色等其他标记，这些非同色系颜色会被自动忽略，不参与对比。
- **修改率**计算基于高亮文本（即被识别为 AI 生成内容的红色文本）字符数的变化
- **行匹配**采用两阶段策略：
  1. 先按 `(模块, 用例名称)` 精确匹配
  2. 剩余行用 `用例名称 + 描述 + 预期` 加权相似度（阈值 0.5）做模糊匹配，避免"模块改名"导致整行被误判成"删除 + 新增"
- 当修改率 > 90% 时控制台会打印告警，提示可能存在未匹配上的同义行
